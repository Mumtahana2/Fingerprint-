This project develops a deep learning and ML based system to predict age and gender from fingerprint images. Gender classification is performed using supervised CNN models trained on labeled data, while age estimation is addressed through an unsupervised pipeline where high-dimensional feature embeddings are reduced via PCA and clustered using K-Means to form latent age groups (e.g., Youth, Adult, Aged).
